# 🚀 Инструкция по запуску TODO App

Полное руководство по запуску микросервисного приложения TODO с авторизацией.

## 📋 Предварительные требования

- Node.js (v18+)
- npm или yarn

## 🔧 Установка и запуск

### Шаг 1: Настройка переменных окружения

⚠️ **Важно**: Оба сервиса должны использовать **одинаковый** `JWT_SECRET`!

#### Auth Service
```bash
cd be/auth
cp .env.example .env
# Отредактируйте .env и установите JWT_SECRET
```

#### Todos Service
```bash
cd be/todos
cp .env.example .env
# Установите ТАКОЙ ЖЕ JWT_SECRET как в Auth Service!
```

**Пример `.env` файла:**
```env
PORT=3000  # или 3001 для Todos
JWT_SECRET=my-super-secret-key-123456
```

### Шаг 2: Установка зависимостей

Откройте **3 терминала** и выполните установку в каждом:

#### Терминал 1 - Auth Service
```bash
cd be/auth
npm install
```

#### Терминал 2 - Todos Service
```bash
cd be/todos
npm install
```

#### Терминал 3 - Frontend
```bash
cd fe
npm install
```

### Шаг 3: Запуск сервисов

В тех же терминалах запустите каждый сервис:

#### Терминал 1 - Auth Service
```bash
cd be/auth
npm run dev
```
✅ Сервис запустится на **http://localhost:3000**

#### Терминал 2 - Todos Service
```bash
cd be/todos
npm run dev
```
✅ Сервис запустится на **http://localhost:3001**

#### Терминал 3 - Frontend
```bash
cd fe
npm run dev
```
✅ Приложение откроется на **http://localhost:5173**

## 🌐 Использование приложения

### 1. Регистрация

1. Откройте **http://localhost:5173**
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя пользователя
   - Email
   - Пароль (минимум 6 символов)
4. После успешной регистрации вы автоматически войдете в систему

### 2. Вход

1. Введите email и пароль
2. Нажмите "Войти"
3. Вы увидите список своих задач

### 3. Работа с задачами

- **Создать задачу**: Нажмите кнопку "+" (Добавить)
- **Отметить выполненной**: Кликните по чекбоксу рядом с задачей
- **Редактировать**: Нажмите кнопку "Редактировать"
- **Удалить**: Нажмите кнопку "Удалить"

### 4. Выход

Нажмите кнопку выхода (иконка logout) в правом верхнем углу

## 🏗 Архитектура

```
┌─────────────────────────────────────────┐
│                                         │
│         Frontend (React + Vite)         │
│         http://localhost:5173           │
│                                         │
└────────────┬──────────────┬─────────────┘
             │              │
             │              │
     ┌───────▼──────┐  ┌────▼──────────┐
     │              │  │               │
     │ Auth Service │  │ Todos Service │
     │   :3000      │  │    :3001      │
     │              │  │               │
     │ - Register   │  │ - CRUD Tasks  │
     │ - Login      │  │ - List Tasks  │
     │ - JWT        │  │               │
     │              │  │               │
     └──────────────┘  └───────────────┘
```

### Порты

- **Frontend**: 5173 (Vite dev server)
- **Auth Service**: 3000 (регистрация, логин, JWT)
- **Todos Service**: 3001 (управление задачами)

### Проксирование

Frontend настроен с проксированием:
- `/api/auth/*` → `http://localhost:3000`
- `/api/tasks/*` → `http://localhost:3001`

## 🔐 Авторизация

### Как это работает?

1. **Регистрация/Вход**:
   - Frontend → Auth Service
   - Auth Service → возвращает JWT токен + данные пользователя

2. **Сохранение токена**:
   - Токен сохраняется в localStorage
   - Токен автоматически добавляется ко всем запросам

3. **Работа с задачами**:
   - Frontend отправляет токен в заголовке `Authorization: Bearer <token>`
   - Todos Service проверяет токен перед каждым запросом
   - Только валидные токены получают доступ к API

4. **Изоляция данных**:
   - Каждый пользователь видит только свои задачи
   - userId извлекается из JWT токена
   - Невозможно получить доступ к чужим задачам

## 🧪 Тестирование API

### Auth Service (curl)

**Регистрация:**
```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "username": "Test User"
  }'
```

**Вход:**
```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123"
  }'
```

### Todos Service (curl)

⚠️ **Важно**: Все запросы к Todos Service требуют JWT токен!

**Получить задачи:**
```bash
# Замените <TOKEN> на реальный токен из ответа /auth/login
curl http://localhost:3001/api/tasks \
  -H "Authorization: Bearer <TOKEN>"
```

**Создать задачу:**
```bash
curl -X POST http://localhost:3001/api/tasks \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TOKEN>" \
  -d '{
    "title": "Купить молоко",
    "description": "В магазине на углу"
  }'
```

**Пример полного workflow:**
```bash
# 1. Зарегистрируйтесь или войдите
TOKEN=$(curl -s -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com", "password": "password123"}' \
  | jq -r '.token')

# 2. Используйте токен для работы с задачами
curl http://localhost:3001/api/tasks \
  -H "Authorization: Bearer $TOKEN"
```

## 📊 Health Checks

Проверка работоспособности сервисов:

```bash
# Auth Service
curl http://localhost:3000/health

# Todos Service
curl http://localhost:3001/health
```

## 🐛 Решение проблем

### Порт уже занят

Если один из портов занят, измените его:

**Auth Service:**
```bash
PORT=3002 npm run dev
```

**Todos Service:**
```bash
PORT=3003 npm run dev
```

Не забудьте обновить `vite.config.ts` с новыми портами!

### Ошибка "Cannot connect to server"

1. Убедитесь, что все 3 сервиса запущены
2. Проверьте, что порты не заняты
3. Проверьте консоль на ошибки

### Ошибка 401 (Unauthorized)

1. Попробуйте выйти и войти снова
2. Проверьте, что токен сохранен (DevTools → Application → Local Storage)

### Задачи не загружаются

1. Проверьте, что Todos Service запущен (порт 3001)
2. Проверьте консоль браузера на ошибки
3. Убедитесь, что в `vite.config.ts` правильно настроен proxy

## 📚 Дополнительная документация

- [be/README.md](./be/README.md) - Документация по бэкенду
- [be/MICROSERVICES.md](./be/MICROSERVICES.md) - Микросервисная архитектура
- [be/auth/README.md](./be/auth/README.md) - Auth Service
- [be/todos/README.md](./be/todos/README.md) - Todos Service
- [fe/README.md](./fe/README.md) - Frontend

## ✅ Checklist запуска

- [ ] Установлены зависимости для всех 3 проектов
- [ ] Auth Service запущен на порту 3000
- [ ] Todos Service запущен на порту 3001
- [ ] Frontend запущен на порту 5173
- [ ] Браузер открыт на http://localhost:5173
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Можно создавать/редактировать/удалять задачи

## 🎉 Готово!

Теперь у вас работает полноценное TODO приложение с:
- ✅ Регистрацией и входом
- ✅ JWT авторизацией
- ✅ Микросервисной архитектурой
- ✅ Современным React UI (Material-UI)
- ✅ TypeScript
- ✅ Zustand для state management

Приятного использования! 🚀

