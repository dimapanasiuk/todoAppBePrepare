# TODO App - Backend Microservices

Бэкенд приложения построен на микросервисной архитектуре с разделением на два независимых сервиса.

## 📁 Структура

```
be/
├── auth/              # 🔐 Сервис авторизации (порт 3000)
│   ├── src/
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
│
├── todos/             # 📝 Сервис задач (порт 3001)
│   ├── src/
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
│
├── README.md          # Этот файл
└── MICROSERVICES.md   # Детальная документация по архитектуре
```

## 🚀 Быстрый старт

### 1. Запуск Redis (обязательно!)

```bash
# Docker (рекомендуется)
docker-compose -f docker-compose.redis.yml up -d

# Или напрямую
docker run -d -p 6379:6379 --name redis redis:alpine
```

### 2. Запуск микросервисов

Каждый микросервис запускается независимо. Откройте два терминала:

#### Терминал 1 - Auth Service

```bash
cd auth
npm install
npm run dev
```

#### Терминал 2 - Todos Service

```bash
cd todos
npm install
npm run dev
```

📖 **Подробнее о Redis**: см. [REDIS_QUICKSTART.md](./REDIS_QUICKSTART.md)

## 🌐 Endpoints

### Auth Service (http://localhost:3000)

- `POST /api/auth/register` - Регистрация пользователя
- `POST /api/auth/login` - Вход в систему (устанавливает httpOnly cookie)
- `POST /api/auth/logout` - Выход из системы (удаляет cookie)
- `GET /api/auth/me` - Получить текущего пользователя (требует cookie)
- `GET /health` - Health check

### Todos Service (http://localhost:3001)

- `GET /api/tasks` - Получить все задачи
- `GET /api/tasks/:id` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу
- `GET /health` - Health check

## 🔄 Как работают сервисы вместе

### 1. Регистрация/Вход пользователя

```
Frontend → Auth Service (POST /api/auth/register или /login)
Auth Service → возвращает { user, token }
```

**Пример запроса:**

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "username": "John Doe"
  }'
```

**Ответ:**

```json
{
  "user": {
    "id": "1",
    "email": "user@example.com",
    "username": "John Doe",
    "createdAt": "2025-11-06T...",
    "updatedAt": "2025-11-06T..."
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

### 2. Работа с задачами

```
Frontend → Todos Service (с токеном в заголовке)
Todos Service → выполняет операцию и возвращает результат
```

**Пример запроса:**

```bash
# Создание задачи
curl -X POST http://localhost:3001/api/tasks \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <your-token>" \
  -d '{
    "title": "Купить молоко",
    "description": "В магазине на углу"
  }'

# Получение всех задач
curl http://localhost:3001/api/tasks
```

### 3. Схема взаимодействия

```
┌─────────────────┐
│                 │
│    Frontend     │
│   (React App)   │
│                 │
└────────┬────────┘
         │
         │ HTTP Requests
         │
         ├──────────────────┬──────────────────┐
         │                  │                  │
         ▼                  ▼                  ▼
┌────────────────┐  ┌────────────────┐  (будущее)
│  Auth Service  │  │ Todos Service  │  Проверка токена
│   Port 3000    │◄─┤   Port 3001    │  между сервисами
│                │  │                │
│ - Register     │  │ - CRUD Tasks   │
│ - Login        │  │ - List Tasks   │
│ - JWT Tokens   │  │                │
└────────────────┘  └────────────────┘
```

### 4. Workflow полного цикла

1. **Пользователь регистрируется:**

   - Frontend → `POST /api/auth/register` → Auth Service
   - Auth Service создает пользователя, хеширует пароль
   - Возвращает JWT токен

2. **Пользователь входит:**

   - Frontend → `POST /api/auth/login` → Auth Service
   - Auth Service проверяет пароль
   - Возвращает JWT токен

3. **Пользователь работает с задачами:**

   - Frontend сохраняет токен (localStorage/memory)
   - Frontend → `POST /api/tasks` (с токеном) → Todos Service
   - Todos Service создает/обновляет/удаляет задачи

4. **Проверка токена (текущая реализация):**

   - Frontend сам проверяет наличие токена
   - Todos Service пока работает без проверки токена (для простоты)

5. **Проверка токена (будущее):**
   - Todos Service может проверять токен через Auth Service
   - Или использовать shared middleware для проверки JWT

## 🚀 Redis Features

### ✅ Реализовано

- ✅ **Token Blacklist** - при logout токен блокируется на 24 часа
- ✅ **Todos Caching** - список задач кешируется на 5 минут
- ✅ **Auto Cache Update** - кеш автоматически обновляется при изменении данных
- ✅ **Shared State** - оба сервиса используют один Redis

### Как это работает

**Token Blacklist (Auth Service):**

```
1. User logout → Token добавляется в Redis blacklist
2. Следующий запрос → Middleware проверяет blacklist
3. Если токен в blacklist → 401 Unauthorized
```

**Todos Caching (Todos Service):**

```
1. GET /api/tasks → Проверяет Redis кеш
2. Если есть в кеше → Возвращает из Redis (быстро!)
3. Если нет → Получает из БД → Сохраняет в Redis
4. CREATE/UPDATE/DELETE → Инвалидирует кеш
```

## 🔐 Безопасность

### ✅ Реализовано

- ✅ Auth Service генерирует JWT токены
- ✅ **HttpOnly cookies** - токен недоступен для JavaScript (защита от XSS)
- ✅ **SameSite=lax** - базовая защита от CSRF атак
- ✅ **Secure flag** в production - cookie только через HTTPS
- ✅ **Token Blacklist** - отозванные токены не работают
- ✅ Todos Service проверяет JWT токен перед каждым запросом
- ✅ Изоляция задач - каждый пользователь видит только свои задачи
- ✅ Logout работает корректно - cookie удаляется на сервере

### Настройка JWT_SECRET

⚠️ **Важно**: Оба сервиса должны использовать **одинаковый** `JWT_SECRET`!

1. Скопируйте `.env.example` в `.env` в каждом сервисе:

```bash
# Auth Service
cd be/auth
cp .env.example .env

# Todos Service
cd be/todos
cp .env.example .env
```

2. Измените `JWT_SECRET` на одинаковое значение в обоих файлах:

```env
JWT_SECRET=ваш-секретный-ключ-123456
```

3. В production используйте надежный случайный ключ!

### Будущие улучшения

- [ ] Refresh tokens
- [x] **Token blacklist/revocation** - реализовано через Redis ✅
- [ ] Rate limiting
- [ ] API Gateway для единой точки входа
- [ ] HTTPS в production

## 📚 Дополнительная документация

- [MICROSERVICES.md](./MICROSERVICES.md) - Детальная документация по микросервисной архитектуре
- [AUTH_ARCHITECTURE.md](./AUTH_ARCHITECTURE.md) - Как работает авторизация (JWT + httpOnly cookies)
- [COOKIE_AUTH_SETUP.md](./COOKIE_AUTH_SETUP.md) - Инструкция по запуску с cookie-based auth
- [MIGRATION_TO_COOKIES.md](./MIGRATION_TO_COOKIES.md) - Что изменилось при переходе на cookies
- [REDIS_QUICKSTART.md](./REDIS_QUICKSTART.md) - 🚀 Быстрый старт с Redis
- [REDIS_SETUP.md](./REDIS_SETUP.md) - Детальная документация Redis интеграции
- [auth/README.md](./auth/README.md) - Документация Auth Service
- [todos/README.md](./todos/README.md) - Документация Todos Service

## 🔮 Дальнейшее развитие

### Phase 1: Защита Todos Service (текущий приоритет)

- [ ] Добавить middleware для проверки JWT в Todos Service
- [ ] Связать задачи с пользователями (userId в Task model)
- [ ] Пользователь видит только свои задачи

### Phase 2: Infrastructure

- [x] **Redis интеграция** - кеширование и blacklist токенов ✅
- [ ] Docker Compose для запуска всех сервисов
- [ ] API Gateway (Kong, Express Gateway)
- [ ] Service Discovery

### Phase 3: Production Ready

- [ ] База данных (PostgreSQL/MongoDB)
- [ ] Логирование (Winston, ELK)
- [ ] Мониторинг (Prometheus, Grafana)
- [ ] CI/CD

## ✅ Преимущества микросервисной архитектуры

✅ **Разделение ответственности** - Auth и Todos независимы
✅ **Легко масштабировать** - каждый сервис можно масштабировать отдельно
✅ **Простота разработки** - можно работать над одним сервисом, не трогая другой
✅ **Изоляция ошибок** - падение одного сервиса не влияет на другой
✅ **Гибкость технологий** - можно использовать разные БД, языки и т.д.
