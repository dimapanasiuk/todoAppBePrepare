# TODO App - Backend Microservices

Бэкенд приложения построен на микросервисной архитектуре с разделением на два независимых сервиса.

## 📁 Структура

```
be/
├── auth/              # 🔐 Сервис авторизации (порт 3000)
├── todos/             # 📝 Сервис задач (порт 3001)
├── docker-compose.yml         # Все сервисы в Docker
├── docker-compose.dev.yml     # Только БД для разработки
├── start-all.sh               # Запуск в режиме разработки
└── start-docker.sh            # Запуск в Docker
```

## 🚀 Быстрый старт

### Режим разработки (рекомендуется)

```bash
cd be
npm run start:all
```

### Режим Docker

```bash
cd be
npm run start:docker
```

## 🌐 Endpoints

### Auth Service (http://localhost:3000)

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход (устанавливает httpOnly cookie)
- `POST /api/auth/logout` - Выход (удаляет cookie)
- `GET /api/auth/me` - Текущий пользователь
- `GET /health` - Health check

### Todos Service (http://localhost:3001)

- `GET /api/tasks` - Получить все задачи
- `GET /api/tasks/:id` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу
- `GET /health` - Health check

## 🗄️ Базы данных

### Заполнение тестовыми данными

```bash
# Создать тестовых пользователей
npm run seed:auth

# Очистить базу данных
npm run clear:auth
```

**Тестовые пользователи:**

- Email: `test@example.com` | Password: `password123`
- Email: `admin@example.com` | Password: `admin123`
- Email: `john@example.com` | Password: `john123`

### Подключение к базам

```bash
# Redis CLI
docker exec -it todo-redis redis-cli

# MongoDB Shell
docker exec -it todo-mongodb mongosh -u admin -p admin123
```

## 🔧 Технологии

- **Express.js** - веб-фреймворк
- **TypeScript** - типизация
- **MongoDB** - основная база данных
- **Mongoose** - ODM для MongoDB
- **Redis** - кеширование и blacklist токенов
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей
- **Docker** - контейнеризация

## 📚 Документация

- [ARCHITECTURE.md](./ARCHITECTURE.md) - Архитектура системы
- [CACHE_STRATEGY.md](./CACHE_STRATEGY.md) - Стратегия кеширования

## 🔐 Безопасность

- ✅ JWT токены в httpOnly cookies
- ✅ Token blacklist при logout
- ✅ Хеширование паролей (bcrypt)
- ✅ Изоляция задач по пользователям
- ✅ CORS настроен для frontend

## 🚀 Команды

```bash
# Разработка
npm run start:all          # Запустить все (БД + сервисы)
npm run dev:auth           # Только Auth Service
npm run dev:todos          # Только Todos Service

# Docker
npm run docker:dev         # Запустить только БД
npm run docker:up          # Запустить все в Docker
npm run docker:down        # Остановить Docker
npm run docker:logs        # Посмотреть логи

# База данных
npm run seed:auth          # Создать тестовых пользователей
npm run clear:auth         # Очистить пользователей

# Сборка
npm run build              # Собрать все сервисы
npm run lint               # Проверить код
npm run format             # Отформатировать код
```

## ✅ Что реализовано

- ✅ Микросервисная архитектура
- ✅ JWT аутентификация через httpOnly cookies
- ✅ Token blacklist при logout
- ✅ MongoDB для персистентного хранения
- ✅ Redis для кеширования
- ✅ Docker контейнеризация
- ✅ TypeScript
- ✅ Изоляция задач по пользователям
