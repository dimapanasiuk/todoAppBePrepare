# TODO List Application

Простое приложение для управления задачами (TODO list) с CRUD операциями.

## Технологии

- **TypeScript** - типизация
- **Node.js** - серверная среда
- **Express** - веб-фреймворк
- Без базы данных (хранение в памяти)

## Возможности

- ✅ Создание задач (Create)
- ✅ Просмотр всех задач (Read)
- ✅ Редактирование задач (Update)
- ✅ Удаление задач (Delete)
- ✅ Отметка задач как выполненных
- ✅ Описание задач (необязательное поле)

## Архитектура

Проект использует **MVC-подобную архитектуру** с разделением на слои:

### 📁 Структура папок

- **`config/`** - конфигурация приложения (порт, окружение, пути)
- **`routes/`** - определение API endpoints и маршрутизация
- **`controllers/`** - обработка HTTP запросов и бизнес-логика
- **`models/`** - работа с данными (in-memory storage)
- **`types/`** - TypeScript интерфейсы и типы
- **`utils/`** - вспомогательные функции
- **`middlewares/`** - middleware функции (обработка ошибок и т.д.)

### 🔄 Поток данных

```
Request → Routes → Controller → Model → Controller → Response
```

1. **Routes** (`routes/`) - принимает HTTP запрос и направляет его в нужный контроллер
2. **Controller** (`controllers/`) - обрабатывает запрос, валидирует данные
3. **Model** (`models/`) - выполняет операции с данными
4. **Response** - контроллер отправляет ответ клиенту

## Установка

```bash
# Установить зависимости
npm install
```

## Запуск

### Режим разработки (с hot reload)
```bash
npm run dev
```

### Сборка и запуск production версии
```bash
npm run build
npm start
```

Сервер запустится на `http://localhost:3000`

## API Endpoints

### Получить все задачи
```
GET /api/tasks
```

### Получить задачу по ID
```
GET /api/tasks/:id
```

### Создать новую задачу
```
POST /api/tasks
Content-Type: application/json

{
  "title": "Название задачи",
  "description": "Описание (необязательно)"
}
```

### Обновить задачу
```
PUT /api/tasks/:id
Content-Type: application/json

{
  "title": "Новое название",
  "description": "Новое описание",
  "completed": true
}
```

### Удалить задачу
```
DELETE /api/tasks/:id
```

## Использование веб-интерфейса

1. Откройте браузер и перейдите на `http://localhost:3000`
2. Используйте форму для добавления новых задач
3. Отмечайте задачи как выполненные с помощью чекбоксов
4. Редактируйте или удаляйте задачи с помощью соответствующих кнопок

## Структура проекта

```
todo/
├── be/                           # Backend (этот проект)
│   ├── src/
│   │   ├── config/              # Конфигурация приложения
│   │   │   └── index.ts
│   │   ├── controllers/         # Контроллеры (бизнес-логика)
│   │   │   └── taskController.ts
│   │   ├── middlewares/         # Middleware функции
│   │   │   └── errorHandler.ts
│   │   ├── models/              # Модели данных (in-memory storage)
│   │   │   └── taskModel.ts
│   │   ├── routes/              # API роуты
│   │   │   ├── index.ts
│   │   │   └── taskRoutes.ts
│   │   ├── types/               # TypeScript типы
│   │   │   └── Task.ts
│   │   ├── utils/               # Утилиты
│   │   │   └── generateId.ts
│   │   └── index.ts             # Главный файл сервера
│   ├── package.json
│   ├── tsconfig.json
│   └── README.md
└── fe/                           # Frontend
    └── public/
        └── index.html           # Веб-интерфейс
```

## Примечания

- Данные хранятся в оперативной памяти и будут утеряны при перезапуске сервера
- Для постоянного хранения данных можно добавить интеграцию с базой данных (MongoDB, PostgreSQL и т.д.)

