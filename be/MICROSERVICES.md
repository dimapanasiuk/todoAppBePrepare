# Микросервисная архитектура

Проект разделен на два независимых микросервиса:

## 📦 Структура

```
be/
├── auth/           # Сервис авторизации
│   ├── src/
│   ├── package.json
│   └── README.md
│
├── todos/          # Сервис управления задачами
│   ├── src/
│   ├── package.json
│   └── README.md
│
└── (старый код)    # Устаревший монолит (можно удалить)
```

## 🔐 Auth Service (Порт 3000)

**Ответственность**: Аутентификация и авторизация пользователей

**Функции**:

- Регистрация пользователей
- Вход в систему
- Генерация и проверка JWT токенов
- Управление пользователями

**API**:

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

**Технологии**:

- Express.js
- TypeScript
- bcrypt (хеширование паролей)
- jsonwebtoken (JWT)

## 📝 Todos Service (Порт 3001)

**Ответственность**: Управление задачами (TODO)

**Функции**:

- CRUD операции для задач (с авторизацией)
- Отметка задач как выполненных
- Хранение задач
- Изоляция данных пользователей
- JWT токен проверка

**API** (все требуют токен):

- `GET /api/tasks` - Список задач текущего пользователя
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить свою задачу
- `DELETE /api/tasks/:id` - Удалить свою задачу

**Технологии**:

- Express.js
- TypeScript
- jsonwebtoken (JWT проверка)

## 🚀 Запуск микросервисов

### Вариант 1: Запуск по отдельности

```bash
# Терминал 1 - Auth Service
cd be/auth
npm install
npm run dev

# Терминал 2 - Todos Service
cd be/todos
npm install
npm run dev
```

### Вариант 2: С помощью скриптов (будущее)

```bash
# В корне be/ можно добавить package.json со скриптами
npm run start:auth
npm run start:todos
npm run start:all
```

## 🔄 Взаимодействие сервисов

```
┌─────────────┐
│   Frontend  │
└──────┬──────┘
       │
       ├──────────┐
       │          │
       ▼          ▼
┌───────────┐ ┌────────────┐
│   Auth    │ │   Todos    │
│ Service   │ │  Service   │
│  :3000    │ │   :3001    │
└───────────┘ └────────────┘
```

**Workflow**:

1. Пользователь регистрируется/входит через **Auth Service**
2. Auth Service возвращает JWT токен
3. Frontend использует токен для запросов к **Todos Service**
4. Todos Service проверяет JWT токен перед каждым запросом
5. Каждый пользователь видит только свои задачи

## 🎯 Преимущества микросервисной архитектуры

✅ **Разделение ответственности** - каждый сервис отвечает за свою область
✅ **Независимое развертывание** - можно обновлять сервисы отдельно
✅ **Масштабируемость** - можно масштабировать каждый сервис независимо
✅ **Изоляция ошибок** - проблема в одном сервисе не влияет на другой
✅ **Технологическая гибкость** - можно использовать разные технологии

## 📊 Health Checks

Каждый сервис имеет endpoint для проверки состояния:

```bash
# Auth Service
curl http://localhost:3000/health

# Todos Service
curl http://localhost:3001/health
```

## ✅ Реализовано

- ✅ JWT авторизация в обоих сервисах
- ✅ Auth middleware в Todos Service
- ✅ Изоляция данных пользователей
- ✅ Валидация JWT токенов
- ✅ Shared JWT_SECRET между сервисами

## 🔮 Будущие улучшения

- [ ] API Gateway для единой точки входа
- [ ] Service Discovery (Consul, Eureka)
- [ ] Refresh tokens
- [ ] Token revocation/blacklist
- [ ] Docker Compose для запуска всех сервисов
- [ ] Message Queue для асинхронной коммуникации (RabbitMQ, Kafka)
- [ ] Centralized logging (ELK Stack)
- [ ] Distributed tracing (Jaeger, Zipkin)
- [ ] Configuration Service
- [ ] Database для каждого сервиса (Database per Service pattern)
- [ ] Rate limiting
- [ ] HTTPS в production
