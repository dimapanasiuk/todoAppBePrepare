# Архитектура TODO App Backend

## 🏗️ Общая схема

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (React)                      │
│                     http://localhost:5173                    │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ HTTP Requests
                         │
         ┌───────────────┴───────────────┐
         │                               │
         ▼                               ▼
┌─────────────────┐            ┌─────────────────┐
│  Auth Service   │            │  Todos Service  │
│  Port: 3000     │            │  Port: 3001     │
│                 │            │                 │
│ - Register      │            │ - CRUD Tasks    │
│ - Login         │            │ - List Tasks    │
│ - Logout        │            │ - Filter Tasks  │
│ - JWT Tokens    │            │                 │
│ - Cookie Auth   │            │                 │
└────────┬────────┘            └────────┬────────┘
         │                               │
         │                               │
         └───────────┬───────────────────┘
                     │
                     │ Shared Infrastructure
                     │
         ┌───────────┴───────────────┐
         │                           │
         ▼                           ▼
┌─────────────────┐        ┌─────────────────┐
│     Redis       │        │    MongoDB      │
│  Port: 6379     │        │  Port: 27017    │
│                 │        │                 │
│ - Token         │        │ - auth_db       │
│   Blacklist     │        │   • users       │
│ - Todos Cache   │        │                 │
│ - Sessions      │        │ - todos_db      │
│                 │        │   • todos       │
└─────────────────┘        └─────────────────┘
```

## 📦 Компоненты

### 1. Auth Service (Port 3000)

**Ответственность:**

- Регистрация пользователей
- Аутентификация (login/logout)
- Генерация JWT токенов
- Управление сессиями через httpOnly cookies
- Blacklist отозванных токенов

**Технологии:**

- Express.js
- TypeScript
- JWT (jsonwebtoken)
- bcrypt (хеширование паролей)
- Redis (blacklist токенов)
- MongoDB (хранение пользователей)

**Endpoints:**

```
POST   /api/auth/register  - Регистрация
POST   /api/auth/login     - Вход (устанавливает cookie)
POST   /api/auth/logout    - Выход (удаляет cookie, blacklist токена)
GET    /api/auth/me        - Текущий пользователь
GET    /health             - Health check
```

### 2. Todos Service (Port 3001)

**Ответственность:**

- CRUD операции с задачами
- Фильтрация и поиск задач
- Кеширование списка задач
- Изоляция задач по пользователям

**Технологии:**

- Express.js
- TypeScript
- JWT (проверка токенов)
- Redis (кеширование)
- MongoDB (хранение задач)

**Endpoints:**

```
GET    /api/tasks          - Список задач
GET    /api/tasks/:id      - Задача по ID
POST   /api/tasks          - Создать задачу
PUT    /api/tasks/:id      - Обновить задачу
DELETE /api/tasks/:id      - Удалить задачу
GET    /health             - Health check
```

### 3. Redis (Port 6379)

**Использование:**

**Token Blacklist (Auth Service):**

```
Key: blacklist:token:{token}
Value: userId
TTL: 24 hours
```

**Todos Cache (Todos Service):**

```
Key: todos:user:{userId}
Value: JSON array of todos
TTL: 5 minutes
```

**Преимущества:**

- ⚡ Быстрая проверка отозванных токенов
- 🚀 Кеширование часто запрашиваемых данных
- 💾 Автоматическое удаление устаревших данных (TTL)

### 4. MongoDB (Port 27017)

**Базы данных:**

**auth_db:**

```javascript
// Collection: users
{
  _id: ObjectId,
  email: String (unique),
  password: String (hashed),
  username: String,
  createdAt: Date,
  updatedAt: Date
}
```

**todos_db:**

```javascript
// Collection: todos
{
  _id: ObjectId,
  userId: String,
  title: String,
  description: String,
  completed: Boolean,
  createdAt: Date,
  updatedAt: Date
}
```

## 🔄 Потоки данных

### Регистрация пользователя

```
1. Frontend → POST /api/auth/register
2. Auth Service:
   - Проверяет email (уникальность)
   - Хеширует пароль (bcrypt)
   - Сохраняет в MongoDB (auth_db.users)
   - Генерирует JWT токен
   - Устанавливает httpOnly cookie
3. Frontend ← { user, token }
```

### Вход пользователя

```
1. Frontend → POST /api/auth/login
2. Auth Service:
   - Находит пользователя в MongoDB
   - Проверяет пароль (bcrypt.compare)
   - Генерирует JWT токен
   - Устанавливает httpOnly cookie
3. Frontend ← { user, token }
```

### Выход пользователя

```
1. Frontend → POST /api/auth/logout (с cookie)
2. Auth Service:
   - Извлекает токен из cookie
   - Добавляет в Redis blacklist (TTL 24h)
   - Удаляет cookie
3. Frontend ← { message: "Logged out" }
```

### Получение задач

```
1. Frontend → GET /api/tasks (с cookie)
2. Todos Service:
   - Проверяет JWT токен из cookie
   - Проверяет blacklist в Redis
   - Проверяет кеш в Redis
   - Если кеш пустой:
     • Получает задачи из MongoDB
     • Сохраняет в Redis (TTL 5 min)
   - Фильтрует по userId
3. Frontend ← [todos]
```

### Создание задачи

```
1. Frontend → POST /api/tasks (с cookie)
2. Todos Service:
   - Проверяет JWT токен
   - Создает задачу в MongoDB
   - Инвалидирует кеш в Redis
3. Frontend ← { todo }
```

## 🔒 Безопасность

### Аутентификация

**JWT Tokens:**

- Подписаны секретным ключом (JWT_SECRET)
- Срок действия: 24 часа
- Содержат: userId, email
- Хранятся в httpOnly cookies

**HttpOnly Cookies:**

```javascript
{
  httpOnly: true,      // Недоступен для JavaScript
  secure: true,        // Только HTTPS (production)
  sameSite: 'lax',     // Защита от CSRF
  maxAge: 24h          // Срок действия
}
```

### Защита от атак

**XSS (Cross-Site Scripting):**

- ✅ HttpOnly cookies (токен недоступен для JS)
- ✅ Санитизация входных данных

**CSRF (Cross-Site Request Forgery):**

- ✅ SameSite cookie attribute
- 🔄 TODO: CSRF tokens для критичных операций

**Token Theft:**

- ✅ Token blacklist при logout
- ✅ Короткий срок действия токенов (24h)
- 🔄 TODO: Refresh tokens

**SQL/NoSQL Injection:**

- ✅ Mongoose ODM (параметризованные запросы)
- ✅ Валидация входных данных

## 📊 Производительность

### Кеширование

**Todos Cache:**

- Время жизни: 5 минут
- Инвалидация: при CREATE/UPDATE/DELETE
- Ускорение: ~10-100x для чтения

**Token Blacklist:**

- Время жизни: 24 часа (срок действия токена)
- Автоматическая очистка (Redis TTL)
- Быстрая проверка: O(1)

### Масштабирование

**Горизонтальное:**

- ✅ Stateless сервисы (можно запустить несколько инстансов)
- ✅ Shared Redis (общее состояние)
- ✅ Shared MongoDB (общая БД)

**Вертикальное:**

- ✅ Redis в памяти (быстрый доступ)
- ✅ MongoDB индексы (быстрый поиск)
- 🔄 TODO: Connection pooling

## 🚀 Развертывание

### Development

```bash
# Запустить все сервисы
cd be
./start-all.sh
```

### Production (TODO)

```
┌─────────────────┐
│   Load Balancer │
│    (Nginx)      │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ Auth 1 │ │ Auth 2 │
└────────┘ └────────┘
    │         │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│Todos 1 │ │Todos 2 │
└────────┘ └────────┘
    │         │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ Redis  │ │MongoDB │
│Cluster │ │Replica │
└────────┘ └────────┘
```

## 📈 Мониторинг

### Health Checks

```bash
# Auth Service
curl http://localhost:3000/health

# Todos Service
curl http://localhost:3001/health

# Redis
docker exec todo-redis redis-cli ping

# MongoDB
docker exec todo-mongodb mongosh --eval "db.runCommand('ping')"
```

### Метрики (TODO)

- Request rate (req/sec)
- Response time (ms)
- Error rate (%)
- Cache hit rate (%)
- Database connections
- Memory usage
- CPU usage

## 🔮 Будущие улучшения

### Phase 1: Безопасность

- [ ] Refresh tokens
- [ ] CSRF protection
- [ ] Rate limiting
- [ ] Input validation (Joi/Zod)

### Phase 2: Производительность

- [ ] Connection pooling
- [ ] Database indexes
- [ ] Query optimization
- [ ] CDN для статики

### Phase 3: Инфраструктура

- [ ] API Gateway
- [ ] Service mesh
- [ ] Distributed tracing
- [ ] Centralized logging

### Phase 4: Features

- [ ] Real-time updates (WebSockets)
- [ ] File uploads
- [ ] Email notifications
- [ ] Search (Elasticsearch)
