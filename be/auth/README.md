# Auth Microservice

Микросервис для аутентификации и авторизации пользователей.

## 🚀 Функционал

- Регистрация пользователей
- Вход в систему (логин)
- JWT токен авторизация
- Получение информации о текущем пользователе
- Безопасное хеширование паролей (bcrypt)

## 📋 API Endpoints

### Authentication

- `POST /api/auth/register` - Регистрация нового пользователя
  ```json
  {
    "email": "user@example.com",
    "password": "password123",
    "username": "John Doe"
  }
  ```

- `POST /api/auth/login` - Вход в систему
  ```json
  {
    "email": "user@example.com",
    "password": "password123"
  }
  ```

- `GET /api/auth/me` - Получить информацию о текущем пользователе (требует JWT токен)
  ```
  Authorization: Bearer <token>
  ```

### Health Check

- `GET /health` - Проверка состояния сервиса

## 🛠 Установка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск в production
npm start
```

## 🌐 Порты

По умолчанию сервис запускается на порту **3000**.

Можно изменить через переменную окружения:
```bash
PORT=3001 npm run dev
```

## 🔐 Переменные окружения

Создайте файл `.env` в корне проекта:

```env
PORT=3000
NODE_ENV=development
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=24h
```

⚠️ **Важно**: В production обязательно измените `JWT_SECRET` на надежный случайный ключ!

## 📦 Структура

```
src/
├── config/         # Конфигурация приложения
├── controllers/    # Контроллеры (бизнес-логика)
├── middlewares/    # Middleware (auth, error handling)
├── models/         # Модели данных
├── routes/         # Маршруты API
├── types/          # TypeScript типы
├── utils/          # Утилиты (JWT, generateId)
└── index.ts        # Точка входа
```

## 🔗 Взаимодействие с другими сервисами

Другие микросервисы могут использовать этот сервис для проверки JWT токенов и авторизации пользователей.

## 🔑 Использование JWT токена

После успешной регистрации или входа, сервис возвращает JWT токен:

```json
{
  "user": {
    "id": "1",
    "email": "user@example.com",
    "username": "John Doe"
  },
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

Используйте этот токен для защищенных запросов:
```
Authorization: Bearer <token>
```



