# Todos Microservice

Микросервис для управления задачами (TODO) с JWT авторизацией.

## 🚀 Функционал

- Создание задач (только для авторизованных пользователей)
- Получение списка задач текущего пользователя
- Обновление своих задач
- Удаление своих задач
- Отметка задач как выполненных
- JWT токен авторизация
- Изоляция задач между пользователями

## 📋 API Endpoints

### Tasks (все требуют JWT токен)

- `GET /api/tasks` - Получить все задачи текущего пользователя
- `GET /api/tasks/:id` - Получить задачу по ID (только свою)
- `POST /api/tasks` - Создать новую задачу
- `PUT /api/tasks/:id` - Обновить задачу (только свою)
- `DELETE /api/tasks/:id` - Удалить задачу (только свою)

**Все endpoints требуют JWT токен в заголовке:**
```
Authorization: Bearer <token>
```

### Health Check

- `GET /health` - Проверка состояния сервиса

## 🛠 Установка

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev

# Сборка проекта
npm run build

# Запуск в production
npm start
```

## 🌐 Порты

По умолчанию сервис запускается на порту **3001**.

Можно изменить через переменную окружения:
```bash
PORT=3002 npm run dev
```

## 📦 Структура

```
src/
├── config/         # Конфигурация приложения
├── controllers/    # Контроллеры (бизнес-логика)
├── models/         # Модели данных
├── routes/         # Маршруты API
├── types/          # TypeScript типы
├── utils/          # Утилиты
└── index.ts        # Точка входа
```

## 🔐 Безопасность

### JWT Авторизация

Каждый запрос к API должен содержать JWT токен в заголовке:

```bash
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Проверка токена

1. Middleware проверяет наличие токена в заголовке
2. Валидирует токен используя JWT_SECRET
3. Извлекает userId из токена
4. Добавляет user данные в request

### Изоляция данных

- Каждый пользователь видит **только свои** задачи
- Невозможно получить/изменить/удалить чужие задачи
- userId автоматически привязывается к задаче при создании

## 🔑 Переменные окружения

Создайте `.env` файл в корне проекта:

```env
PORT=3001
NODE_ENV=development
JWT_SECRET=your-super-secret-key-change-in-production
```

⚠️ **Важно**: `JWT_SECRET` должен быть **одинаковым** в Auth Service и Todos Service!

## 🔗 Взаимодействие с другими сервисами

Этот сервис использует тот же `JWT_SECRET`, что и **Auth Service**, для проверки токенов.

### Пример использования:

1. Пользователь получает токен от Auth Service
2. Frontend отправляет токен в заголовке запроса к Todos Service
3. Todos Service проверяет токен и выполняет операцию

